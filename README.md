# NODUS_s

Монолитное FastAPI приложение для чата, обмена сообщениями и работы с вложениями.

## 🚀 Быстрый старт

### Требования
- Python 3.12+
- Docker & Docker Compose
- PostgreSQL 16
- Redis 7
- MinIO (S3-совместимое хранилище)

### Установка и запуск

#### Автоматическая инициализация

**Windows (PowerShell):**
```powershell
.\scripts\init.ps1
```

**Linux/macOS:**
```bash
chmod +x scripts/init.sh
./scripts/init.sh
```

#### Ручная установка

1. **Клонировать репозиторий**
```bash
git clone <repository-url>
cd NODUS_s
```

2. **Создать .env файл**
```bash
cp .env.example .env
# Отредактируйте .env и укажите правильные значения
```

3. **Запустить инфраструктуру**
```bash
docker-compose up -d
```

4. **Установить зависимости**
```bash
pip install -e .
```

5. **Применить миграции**
```bash
alembic upgrade head
```

6. **Запустить приложение**
```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

7. **Запустить RQ Worker (опционально, для обработки аудио)**
```bash
rq worker audio --url redis://localhost:6379/1
```

## 📚 API Документация

После запуска приложения документация доступна по адресам:
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

## 🏗 Архитектура

### Структура проекта
```
app/
├── api/              # API endpoints и middleware
│   ├── v1/          # API версии 1
│   │   ├── auth.py       # Аутентификация
│   │   ├── users.py      # Пользователи
│   │   ├── chats.py      # Чаты
│   │   ├── messages.py   # Сообщения
│   │   └── attachments.py # Вложения
│   ├── dependencies.py   # Зависимости FastAPI
│   └── ws.py            # WebSocket
├── core/            # Ядро приложения
├── domain/          # Модели данных
├── repositories/    # Слой доступа к данным
├── schemas/         # Pydantic схемы
├── services/        # Бизнес-логика
└── workers/         # Фоновые задачи
```

### Технологический стек
- **Backend**: FastAPI, SQLAlchemy (async), Pydantic
- **Database**: PostgreSQL 16
- **Cache**: Redis 7
- **Queue**: RQ (Redis Queue)
- **Storage**: MinIO (S3-compatible)
- **Auth**: JWT (custom implementation)

## 🔐 Безопасность

- JWT токены с access и refresh механизмом
- Bcrypt для хэширования паролей
- Идемпотентность критических операций
- CORS middleware (настройте для продакшена)

## 🧪 Тестирование

```bash
# Установить тестовые зависимости
pip install -e ".[test]"

# Запустить тесты
pytest
```

## 🐳 Docker

### Запуск инфраструктуры
```bash
docker-compose up -d
```

### Остановка
```bash
docker-compose down
```

### Очистка данных
```bash
docker-compose down -v
```

## 📝 Переменные окружения

См. `.env.example` для списка всех доступных переменных.

Основные переменные:
- `DATABASE_URL` - URL подключения к PostgreSQL
- `REDIS_URL` - URL подключения к Redis
- `JWT_SECRET_KEY` - Секретный ключ для JWT
- `S3_ENDPOINT_URL` - URL MinIO/S3

## 🔄 Миграции

### Создать новую миграцию
```bash
alembic revision --autogenerate -m "description"
```

### Применить миграции
```bash
alembic upgrade head
```

### Откатить миграцию
```bash
alembic downgrade -1
```

## 📊 Мониторинг

- **MinIO Console**: http://localhost:9001
  - Username: minioadmin
  - Password: minioadmin

## 🤝 Разработка

### Code Style
- Используется Ruff для линтинга
- Line length: 100 символов
- Target: Python 3.12

### Commit Convention
Используйте conventional commits:
- `feat:` - новая функциональность
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов

## 📄 Лицензия

Apache License 2.0

## 👥 Команда

Разработано командой NODUS

# 📦 Deployment Files

Конфигурационные файлы и скрипты для деплоя NODUS Backend на production сервер.

## 📄 Файлы

### `nginx.conf`
Конфигурация Nginx для проксирования запросов к FastAPI приложению.
- HTTP/HTTPS поддержка
- WebSocket поддержка для `/ws` endpoint
- Настройки для больших файлов (до 100MB)

### `nodus-backend.service`
Systemd service файл для автозапуска приложения.
- Автоматический перезапуск при сбое
- Логирование в `/var/log/nodus-backend/`
- 4 worker процесса (можно настроить)

### `deploy.sh`
Скрипт для деплоя обновлений.
```bash
bash deploy/deploy.sh
```

### `quick-start.sh`
Автоматический скрипт установки с нуля (для новых серверов).
```bash
# Как root
curl -sSL https://raw.githubusercontent.com/your-repo/NODUS_s/main/deploy/quick-start.sh | bash

# Или локально
sudo bash deploy/quick-start.sh
```

## 📖 Документация

Полная инструкция по деплою находится в корне проекта: [`DEPLOYMENT.md`](../DEPLOYMENT.md)

## 🚀 Быстрый старт

### Вариант 1: Автоматическая установка (рекомендуется для новых серверов)
```bash
sudo bash deploy/quick-start.sh
```

### Вариант 2: Ручная установка (полный контроль)
Следуйте инструкциям в [`DEPLOYMENT.md`](../DEPLOYMENT.md)

### Вариант 3: Обновление существующей установки
```bash
cd /home/nodus/NODUS_s
bash deploy/deploy.sh
```

## 🔧 Требования

- Ubuntu 22.04 LTS
- Root или sudo доступ
- Минимум 2GB RAM
- 20GB свободного места

## 📞 Поддержка

При проблемах с деплоем:
1. Проверьте логи: `sudo journalctl -u nodus-backend -f`
2. Проверьте статус: `sudo systemctl status nodus-backend`
3. Смотрите troubleshooting раздел в [`DEPLOYMENT.md`](../DEPLOYMENT.md)

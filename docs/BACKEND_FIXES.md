# 🔧 Исправления бэкенда

**Дата:** 2024-11-04  
**Автор:** Backend team

## Проблемы, которые были исправлены

### ✅ Проблема 1: Сообщения не появляются без перезагрузки
**Причина:** WebSocket события отправлялись в общий канал, а не персональные каналы участников чата.

**Исправление:**
- Изменена архитектура WebSocket на персональные каналы: `ws:user:{user_id}`
- Добавлен метод `list_participant_ids` в `ChatMemberRepository`
- Обновлен `MessageService._publish_event` для отправки событий всем участникам чата

**Файлы:**
- `app/api/ws.py` - теперь каждый пользователь подписывается на свой канал
- `app/services/message.py` - отправка событий всем участникам
- `app/repositories/chat.py` - метод получения списка участников

### ✅ Проблема 2: При перезагрузке перенаправляет на логин
**Причина:** WebSocket endpoint не требовал аутентификации, токен не проверялся.

**Исправление:**
- Добавлена аутентификация по токену из query параметра
- Токен валидируется перед установкой WebSocket соединения
- При невалидном токене возвращается код `1008 Policy Violation`

**Формат подключения:**
```javascript
ws://localhost:8000/ws?token=<access_token>
```

**Файлы:**
- `app/api/ws.py` - добавлена функция `authenticate_websocket`

### ✅ Проблема 3: Отсутствует поле participants в чатах
**Причина:** Схема `ChatRead` не содержала список участников.

**Исправление:**
- Добавлено поле `participants: list[UserRead]` в `ChatRead`
- Реализовано автоматическое извлечение участников из relationship `members`
- Добавлен eager loading для корректной загрузки данных

**Файлы:**
- `app/schemas/chat.py` - добавлено поле `participants`
- `app/repositories/chat.py` - eager loading через `joinedload`
- `app/services/chat.py` - перезагрузка чата с участниками после commit

---

## Технические детали

### WebSocket архитектура

**До:**
```python
# Все пользователи подписывались на один канал
await pubsub.subscribe("ws:messages")
# Все получали все события
await redis.publish("ws:messages", json.dumps(payload))
```

**После:**
```python
# Каждый пользователь подписывается на персональный канал
channel = f"ws:user:{user_id}"
await pubsub.subscribe(channel)

# События отправляются только участникам чата
for user_id in participant_ids:
    channel = f"ws:user:{user_id}"
    await redis.publish(channel, payload_json)
```

### Формат WebSocket событий

События отправляются в формате:
```json
{
  "event": "message.created",
  "data": {
    "id": 123,
    "chat_id": 456,
    "author_id": 789,
    "type": "text",
    "content": "Hello!",
    "payload": null,
    "ts": "2024-11-04T14:30:00Z"
  }
}
```

### Формат ответа чатов

Теперь все endpoint'ы возвращают `participants`:
```json
{
  "id": 1,
  "title": "Chat Title",
  "is_group": false,
  "created_at": "2024-11-04T12:00:00Z",
  "participants": [
    {
      "id": 1,
      "email": "user1@example.com",
      "display_name": "Alice",
      "tag": "alice",
      "avatar_url": null,
      "created_at": "2024-11-04T10:00:00Z"
    },
    {
      "id": 2,
      "email": "user2@example.com",
      "display_name": "Bob",
      "tag": "bob",
      "avatar_url": null,
      "created_at": "2024-11-04T11:00:00Z"
    }
  ]
}
```

---

## Как протестировать

### 1. Проверить WebSocket аутентификацию

```javascript
// В браузере DevTools Console
const token = "YOUR_ACCESS_TOKEN";
const ws = new WebSocket(`ws://localhost:8000/ws?token=${token}`);
ws.onopen = () => console.log('✅ Connected');
ws.onerror = (e) => console.error('❌ Error:', e);
ws.onmessage = (e) => console.log('📨 Message:', e.data);
```

### 2. Проверить отправку сообщений двум пользователям

**Терминал 1 (Alice):**
```bash
TOKEN1="alice_token"
wscat -c "ws://localhost:8000/ws?token=$TOKEN1"
```

**Терминал 2 (Bob):**
```bash
TOKEN2="bob_token"
wscat -c "ws://localhost:8000/ws?token=$TOKEN2"
```

**Терминал 3 (отправка сообщения):**
```bash
curl -X POST http://localhost:8000/api/v1/messages \
  -H "Authorization: Bearer $TOKEN1" \
  -H "Content-Type: application/json" \
  -H "Idempotency-Key: $(uuidgen)" \
  -d '{
    "chat_id": 1,
    "type": "text",
    "content": "Hello Bob!"
  }'
```

**Ожидаемый результат:**
- ✅ В терминале Alice появляется событие
- ✅ В терминале Bob **ТОЖЕ** появляется событие
- ✅ Оба получают одинаковый JSON с `event: "message.created"`

### 3. Проверить поле participants

```bash
curl http://localhost:8000/api/v1/chats \
  -H "Authorization: Bearer $TOKEN"
```

**Ожидаемый результат:**
- ✅ Каждый чат содержит поле `participants`
- ✅ `participants` это массив объектов пользователей
- ✅ Каждый участник имеет поля: `id`, `email`, `display_name`, `tag`, `avatar_url`, `created_at`

---

## Логирование

Добавлено информативное логирование для отладки:

```
INFO:app.api.ws:WebSocket connected: user_id=1
INFO:app.services.message:Broadcast message.created to 2 participants in chat 1
DEBUG:app.services.message:Published message.created to user 1 for message 123
DEBUG:app.services.message:Published message.created to user 2 for message 123
INFO:app.api.ws:WebSocket disconnected: user_id=1
```

---

## Следующие шаги

Фронтенд команда может:
1. ✅ Обновить WebSocket подключение для передачи токена в query
2. ✅ Использовать поле `participants` вместо создания его на фронтенде
3. ✅ Проверить, что события приходят в реальном времени обоим собеседникам

---

## Контакты

Если есть вопросы или проблемы:
- Проверьте логи сервера
- Убедитесь, что Redis запущен и доступен
- Убедитесь, что токен валидный и не истек

**Версия:** 1.0.0

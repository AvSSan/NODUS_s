# 🚀 Быстрый старт для Frontend команды

## Добро пожаловать в NODUS_s!

Этот документ содержит всю необходимую информацию для начала разработки frontend приложения.

---

## 📋 Что нужно знать

### Backend готов и работает
✅ Все API endpoints реализованы и протестированы  
✅ WebSocket для real-time обновлений работает  
✅ JWT аутентификация настроена  
✅ CORS включен (разрешены все origin'ы для разработки)  
✅ Идемпотентность критических операций работает  
✅ База данных с миграциями готова  

---

## 🔗 Основные URL'ы

```
Backend API:     http://localhost:8000/api/v1
WebSocket:       ws://localhost:8000/ws
Swagger UI:      http://localhost:8000/docs
ReDoc:           http://localhost:8000/redoc
MinIO Console:   http://localhost:9001 (admin/admin)
```

---

## ⚡ Быстрый запуск Backend

### Windows (PowerShell)
```powershell
cd c:\Codes\Git\NODUS_s
.\scripts\init.ps1
```

### Linux/macOS
```bash
cd NODUS_s
chmod +x scripts/init.sh
./scripts/init.sh
```

После инициализации:
```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

---

## 📚 Документация

### 1. **API Документация** (`docs/FRONTEND_API.md`)
- Подробное описание всех endpoints
- Примеры запросов и ответов
- Обработка ошибок
- WebSocket события
- Типичные сценарии использования

### 2. **TypeScript примеры** (`docs/TYPESCRIPT_EXAMPLES.md`)
- Готовые TypeScript типы
- React hooks для работы с API
- Сервисы для всех endpoints
- WebSocket менеджер с реконнектом
- Примеры компонентов

### 3. **Схема БД** (`docs/DATABASE_SCHEMA.md`)
- ER диаграмма
- Описание всех таблиц
- Примеры SQL запросов

---

## 🎯 Основные концепции

### 1. Аутентификация
```typescript
// Регистрация/Логин возвращает:
{
  access_token: string,   // Живет 15 минут
  refresh_token: string,  // Живет 7 дней
  token_type: "bearer",
  expires_in: 900
}

// Используйте access_token в заголовках:
Authorization: Bearer <access_token>
```

### 2. Idempotency Key
```typescript
// Для POST, PATCH, DELETE требуется уникальный UUID:
headers: {
  'Idempotency-Key': crypto.randomUUID()
}
```

### 3. WebSocket события
```typescript
// Сервер отправляет события в формате:
{
  event: "message.created" | "message.updated",
  data: Message
}
```

---

## 📦 Готовые к использованию компоненты

### TypeScript типы
Все типы данных готовы в `docs/TYPESCRIPT_EXAMPLES.md`:
- `User`, `Chat`, `Message`
- `TokenPair`, `AuthResponse`
- `WSEvent`

### Сервисы
- `AuthService` - регистрация, логин, refresh
- `ChatService` - CRUD чатов
- `MessageService` - отправка, редактирование сообщений
- `AttachmentService` - загрузка файлов
- `WebSocketService` - real-time обновления

### React Hooks
- `useAuth()` - управление пользователем
- `useChats()` - список чатов
- `useMessages(chatId)` - сообщения чата
- `useWebSocket(handler)` - WebSocket подписка

---

## 🔑 Основные Endpoints

### Аутентификация
```
POST /auth/register  - Регистрация
POST /auth/login     - Вход
POST /auth/refresh   - Обновление токена
```

### Пользователи
```
GET /users/me        - Текущий пользователь
```

### Чаты
```
GET    /chats           - Список чатов
POST   /chats           - Создать чат
GET    /chats/{id}      - Получить чат
PATCH  /chats/{id}      - Обновить чат
DELETE /chats/{id}      - Удалить чат
```

### Сообщения
```
GET   /messages?chat_id={id}  - Получить сообщения
POST  /messages               - Отправить сообщение
PATCH /messages/{id}          - Редактировать сообщение
```

### Вложения
```
POST /attachments  - Получить pre-signed URL для загрузки
```

---

## 💡 Типичные задачи

### Задача 1: Регистрация и вход
```typescript
// 1. Регистрация
const { user, tokens } = await authService.register({
  email: 'user@example.com',
  password: 'password',
  display_name: 'John Doe'
});

// 2. Сохранить токены
localStorage.setItem('access_token', tokens.access_token);
localStorage.setItem('refresh_token', tokens.refresh_token);

// 3. Получить профиль
const me = await authService.getCurrentUser();
```

### Задача 2: Показать список чатов
```typescript
const { chats, loading } = useChats();

// Отображение
{loading ? <Loader /> : (
  <ul>
    {chats.map(chat => (
      <li key={chat.id}>{chat.title}</li>
    ))}
  </ul>
)}
```

### Задача 3: Отправить сообщение
```typescript
const { sendMessage } = useMessages(chatId);

await sendMessage('text', 'Hello!');
```

### Задача 4: Получать сообщения в реальном времени
```typescript
useWebSocket((event) => {
  if (event.event === 'message.created') {
    addMessageToChat(event.data);
  }
});
```

---

## 🧪 Тестирование API

### Через Swagger UI
1. Откройте http://localhost:8000/docs
2. Зарегистрируйте пользователя через `/auth/register`
3. Скопируйте `access_token`
4. Нажмите кнопку "Authorize" вверху страницы
5. Вставьте токен в формате: `Bearer <token>`
6. Тестируйте любые endpoints

### Через cURL
```bash
# Регистрация
curl -X POST http://localhost:8000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -H "Idempotency-Key: $(uuidgen)" \
  -d '{
    "email": "test@example.com",
    "password": "password123",
    "display_name": "Test User"
  }'

# Получить чаты
curl http://localhost:8000/api/v1/chats \
  -H "Authorization: Bearer <token>"
```

---

## 🎨 Рекомендуемый UI/UX Flow

### 1. Страница регистрации/входа
- Email + Password + Display Name
- Валидация на клиенте
- После успеха → сохранить токены → редирект на /chats

### 2. Список чатов
- Показать все чаты пользователя
- Отображать last_message (можно добавить позже)
- Кнопка "Создать чат"
- При клике → открыть окно чата

### 3. Окно чата
- История сообщений (обратный порядок, новые внизу)
- Поле ввода с кнопкой отправки
- WebSocket для real-time обновлений
- Поддержка текстовых и голосовых сообщений

### 4. Голосовые сообщения
- Запись через MediaRecorder API
- Конвертация в Opus (если возможно)
- Загрузка через pre-signed URL
- Отправка как сообщение с payload

---

## ⚠️ Важные замечания

### 1. Токены
- Access token живет **15 минут**
- Обрабатывайте 401 ошибки и делайте refresh
- Используйте interceptor (пример в TypeScript документации)

### 2. Idempotency
- **Обязателен** для POST, PATCH, DELETE
- Используйте `crypto.randomUUID()`
- Не переиспользуйте один ключ для разных запросов

### 3. WebSocket
- Реализуйте реконнект при разрыве
- Обрабатывайте только события для текущего чата
- Не дублируйте сообщения (проверяйте ID)

### 4. Безопасность
- Храните токены в localStorage или httpOnly cookies
- Не показывайте raw токены в UI
- Логаут = удаление токенов из хранилища

---

## 🐛 Отладка

### Проверить статус Backend
```bash
curl http://localhost:8000/health
# Ответ: {"status": "ok"}
```

### Проверить WebSocket
```javascript
const ws = new WebSocket('ws://localhost:8000/ws');
ws.onopen = () => console.log('Connected');
ws.onmessage = (e) => console.log('Message:', e.data);
```

### Логи Backend
Backend выводит логи всех запросов в консоль при запуске с `--reload`.

---

## 📞 Поддержка

### Где найти помощь:
1. **Swagger UI**: http://localhost:8000/docs - интерактивная документация
2. **Документация**: `docs/FRONTEND_API.md` - полное описание API
3. **Примеры**: `docs/TYPESCRIPT_EXAMPLES.md` - готовый код
4. **Backend команда**: создайте issue или обратитесь напрямую

### Частые проблемы:

**401 Unauthorized**
- Проверьте формат токена: `Bearer <token>`
- Токен истек? Используйте refresh endpoint

**409 Conflict**
- Idempotency-Key уже использован
- Сгенерируйте новый UUID

**403 Forbidden**
- Пользователь не является участником чата
- Проверьте chat_id и права доступа

**CORS ошибка**
- Backend должен быть запущен
- CORS настроен на разрешение всех origin'ов

---

## 🎯 Следующие шаги

1. ✅ Изучите документацию API (`docs/FRONTEND_API.md`)
2. ✅ Скопируйте TypeScript типы и сервисы
3. ✅ Запустите Backend локально
4. ✅ Протестируйте endpoints через Swagger UI
5. ✅ Реализуйте регистрацию/вход
6. ✅ Подключите WebSocket
7. ✅ Создайте UI для чатов и сообщений

---

## 📦 Дополнительные ресурсы

### Технологии Backend
- **FastAPI**: https://fastapi.tiangolo.com/
- **PostgreSQL**: https://www.postgresql.org/docs/
- **Redis**: https://redis.io/docs/
- **MinIO**: https://min.io/docs/minio/linux/index.html

### Рекомендуемые библиотеки для Frontend
- **Axios** или **Fetch API** - HTTP запросы
- **React Query** - кэширование и управление состоянием
- **Zustand** или **Redux** - глобальное состояние
- **Socket.io-client** (опционально) - альтернатива нативному WebSocket

---

## 🎉 Удачи в разработке!

Backend готов к интеграции. Все endpoints работают, документация полная, примеры кода готовы.

**Если возникнут вопросы - обращайтесь к Backend команде!**

---

**Версия:** 1.0.0  
**Дата:** 2024-03-26  
**Backend API:** v1

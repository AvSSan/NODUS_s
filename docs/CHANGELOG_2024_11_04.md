# 📋 Changelog - 2024-11-04

## 🔧 Критические исправления

### WebSocket: Аутентификация и персональные каналы

**Проблема:** Получатели не видят новые сообщения без перезагрузки страницы.

**Корневая причина:** 
1. WebSocket endpoint не требовал аутентификации
2. Все пользователи подписывались на один общий канал `ws:messages`
3. События отправлялись в общий канал, что не гарантировало доставку конкретным пользователям

**Решение:**
- ✅ Добавлена аутентификация через токен в query параметре
- ✅ Реализованы персональные каналы для каждого пользователя: `ws:user:{user_id}`
- ✅ События теперь отправляются всем участникам чата в их персональные каналы

**Измененные файлы:**
- `app/api/ws.py` - полная переработка с аутентификацией
- `app/services/message.py` - broadcast событий участникам
- `app/repositories/chat.py` - метод `list_participant_ids()`

---

### API: Добавлено поле participants в чаты

**Проблема:** Фронтенд не может отобразить имена участников чата.

**Корневая причина:** Схема `ChatRead` не содержала список участников.

**Решение:**
- ✅ Добавлено поле `participants: list[UserRead]` в схему `ChatRead`
- ✅ Реализовано автоматическое извлечение данных из relationship
- ✅ Добавлен eager loading для оптимизации запросов

**Измененные файлы:**
- `app/schemas/chat.py` - расширена схема `ChatRead`
- `app/repositories/chat.py` - добавлен `joinedload` для `members.user`
- `app/services/chat.py` - корректная перезагрузка с participants

---

### Аутентификация: Исправлена проблема с редиректом на логин

**Проблема:** При перезагрузке страницы происходит редирект на страницу авторизации.

**Корневая причина:** WebSocket не требовал токен, что приводило к рассинхронизации состояния.

**Решение:**
- ✅ WebSocket теперь требует валидный токен
- ✅ При невалидном токене возвращается код `1008 Policy Violation`
- ✅ Фронтенд может корректно обрабатывать ошибки аутентификации

**Измененные файлы:**
- `app/api/ws.py` - функция `authenticate_websocket()`

---

## 📊 Статистика изменений

- **Файлов изменено:** 6
- **Строк добавлено:** ~150
- **Строк удалено:** ~30
- **Новых методов:** 3
  - `authenticate_websocket()` в `ws.py`
  - `list_participant_ids()` в `ChatMemberRepository`
  - `model_validate()` override в `ChatRead`

---

## 🧪 Тестирование

Созданы документы:
- `docs/BACKEND_FIXES.md` - описание исправлений
- `docs/TESTING_GUIDE.md` - руководство по тестированию

Рекомендуется протестировать:
1. WebSocket подключение с токеном
2. Получение событий обоими участниками чата
3. Наличие поля `participants` в API ответах
4. Работу endpoint `/users/me`

---

## 🔄 Миграции

Миграции не требуются - все изменения только в коде приложения.

---

## ⚠️ Breaking Changes

### WebSocket подключение

**Было:**
```javascript
new WebSocket('ws://localhost:8000/ws');
```

**Стало:**
```javascript
const token = getAccessToken();
new WebSocket(`ws://localhost:8000/ws?token=${token}`);
```

⚠️ **Фронтенд должен обновить код подключения к WebSocket**

---

## 📝 Документация

Обновлена документация:
- README.md - без изменений
- BACKEND_CHECKLIST.md - уже содержал требования
- Добавлены новые файлы:
  - `docs/BACKEND_FIXES.md`
  - `docs/TESTING_GUIDE.md`
  - `docs/CHANGELOG_2024_11_04.md`

---

## 🎯 Следующие шаги

Для фронтенд команды:
1. Обновить WebSocket подключение для передачи токена
2. Использовать поле `participants` из API вместо создания на клиенте
3. Протестировать обмен сообщениями в реальном времени
4. Проверить обработку ошибок WebSocket (код 1008)

---

## 👥 Авторы

- Backend team

---

## 🔗 Связанные задачи

- Исправление WebSocket событий
- Добавление поля participants
- Улучшение аутентификации

---

**Версия:** 1.0.0  
**Статус:** ✅ Готово к тестированию
